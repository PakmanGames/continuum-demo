FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir flask

# The server proxies /api/* to the backend, so the page never needs to know
# which port the backend is published on.
COPY frontend_server.py .
COPY frontend/ ./static/

EXPOSE 3000

CMD ["python", "frontend_server.py"]
